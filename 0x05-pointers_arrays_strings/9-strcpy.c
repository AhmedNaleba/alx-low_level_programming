#include "main.h"

/**
 * _strcpy - Copy string
 * @dest: First operand
 * @src: Second operand
 *
 * Return: the pointer to dest
 */

char *_strcpy(char *dest, char *src)
{
	int a, str;

	for (str = 0; src[str] != '\0'; str++)
	{

	}

	for (a = 0; a <= str; a++)
	{

		dest[a] = src[a];
	}
			return (dest);

}

