#include "main.h"

/**
 * _isupper - Checks if letter is upper or lowercase
 * @c: Letter to be checked
 *
 * Return: 0.
 */

int _isupper(int c)
{
	if (c >= 'A' && c <= 'Z')
	{
		return (1);
		c++;
	}
	else
	{
		return (0);
		c++;
	}

	return (0);
}

