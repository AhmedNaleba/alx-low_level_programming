#include "main.h"

/**
 * _isdigit - Checks for digit
 * @c: Character to be checked
 *
 * Return: 0
 */

int _isdigit(int c)
{
	if (c >= '0' && c <= '9')
	{
		return (1);
		c++;
	}
	else
	{
		return (0);
		c++;
	}

	return (0);
}

